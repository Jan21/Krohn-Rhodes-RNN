defaults:
  - model: automaton_rnn
  - data: automaton_data
  - trainer: default
  - logger: wandb
  - _self_

seed: 42
experiment_name: "rnn_automaton_generalization"

extraction:
  every_n_epochs: 5
  out_dir: "snapshots"
  eps: 0.2
  max_len: 8
  cap_per_level: 200